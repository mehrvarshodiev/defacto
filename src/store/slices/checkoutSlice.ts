import {createSlice,PayloadAction} from '@reduxjs/toolkit'; import type {Locale,Delivery,Payment} from '../../types/product'; interface State{locale:Locale;delivery:Delivery;payment:Payment;name:string;phone:string;address:string;note:string}; const initial:State={locale:'ru',delivery:'express',payment:'cash',name:'',phone:'',address:'',note:''}; const slice=createSlice({name:'checkout',initialState:initial,reducers:{locale:(s,a:PayloadAction<Locale>)=>{s.locale=a.payload},delivery:(s,a:PayloadAction<Delivery>)=>{s.delivery=a.payload},payment:(s,a:PayloadAction<Payment>)=>{s.payment=a.payload},field:(s,a:PayloadAction<Partial<State>>)=>Object.assign(s,a.payload),reset:()=>initial}}); export const {locale,delivery,payment,field,reset}=slice.actions; export default slice.reducer;