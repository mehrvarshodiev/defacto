import {createSlice,PayloadAction} from '@reduxjs/toolkit'; import type {Category} from '../../types/product';
interface State{category:Category|'all';size:string;color:string;max:number;sort:'featured'|'asc'|'desc'} const initial:State={category:'all',size:'all',color:'all',max:1500,sort:'featured'};
const slice=createSlice({name:'filters',initialState:initial,reducers:{category:(s,a:PayloadAction<State['category']>)=>{s.category=a.payload},size:(s,a:PayloadAction<string>)=>{s.size=a.payload},color:(s,a:PayloadAction<string>)=>{s.color=a.payload},max:(s,a:PayloadAction<number>)=>{s.max=a.payload},sort:(s,a:PayloadAction<State['sort']>)=>{s.sort=a.payload},reset:()=>initial}}); export const {category,size,color,max,sort,reset}=slice.actions; export default slice.reducer;