import {useMemo} from 'react'; import {products} from '../data/products'; import {useAppSelector} from '../store/hooks'; export default function useProducts(){const f=useAppSelector(s=>s.filters);return useMemo(()=>products.filter(p=>(f.category==='all'||p.category===f.category)&&(f.size==='all'||p.sizes.includes(f.size))&&(f.color==='all'||p.colors.includes(f.color))&&p.price<=f.max).sort((a,b)=>f.sort==='asc'?a.price-b.price:f.sort==='desc'?b.price-a.price:0),[f]);}