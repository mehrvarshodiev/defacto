import {Heart,Plus} from 'lucide-react'; import {Link} from 'react-router-dom'; import type {Product,Locale} from '../../types/product'; import {useAppDispatch,useAppSelector} from '../../store/hooks'; import {add} from '../../store/slices/cartSlice';
export default function ProductCard({p,locale}:{p:Product;locale:Locale}){const d=useAppDispatch(); const wished=useAppSelector(s=>false); return <article className="group"><div className="relative overflow-hidden rounded-3xl bg-soft"><Link to={`/product/${p.slug}`}><img src={p.image} alt={p.name[locale]} loading="lazy" className="aspect-[4/5] w-full object-cover transition duration-500 group-hover:scale-[1.03]"/></Link>{p.sale&&<span className="absolute left-3 top-3 rounded-full bg-accent px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-white">Sale</span>}<button onClick={()=>d(add({product:p,size:p.sizes[Math.floor(p.sizes.length/2)],color:p.colors[0],quantity:1}))} className="absolute bottom-3 right-3 grid h-10 w-10 place-items-center rounded-full bg-white shadow-soft sm:opacity-0 sm:transition sm:group-hover:opacity-100"><Plus size={18}/></button><span className={`absolute right-3 top-3 rounded-full bg-white/90 p-2 ${wished?'text-accent':'text-brand'}`}><Heart size={17}/></span></div><Link to={`/product/${p.slug}`} className="mt-3 block"><p className="text-[10px] uppercase tracking-[.2em] text-black/40">{p.collection}</p><h3 className="mt-1 font-semibold">{p.name[locale]}</h3><div className="mt-1 flex gap-2 text-sm"><b>{p.price} TJS</b>{p.oldPrice&&<span className="text-black/35 line-through">{p.oldPrice} TJS</span>}</div></Link></article>}